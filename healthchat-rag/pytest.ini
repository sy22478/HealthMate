[tool:pytest]
# Environment variables for tests
env =
    HEALTHMATE_OPENAI_API_KEY=test-openai-key
    HEALTHMATE_PINECONE_API_KEY=test-pinecone-key
    HEALTHMATE_PINECONE_ENVIRONMENT=test-environment
    HEALTHMATE_PINECONE_INDEX_NAME=test-index
    HEALTHMATE_POSTGRES_URI=sqlite:///./test.db
    HEALTHMATE_SECRET_KEY=test-secret-key-for-testing-only
    HEALTHMATE_ENVIRONMENT=test

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output and reporting
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
    --durations=10
    --maxfail=5
    --cov=app
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=xml
    --cov-fail-under=80
    -n auto

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    auth: Authentication related tests
    health: Health data related tests
    chat: Chat/AI related tests
    security: Security related tests
    database: Database related tests
    api: API endpoint tests
    utils: Utility function tests

# Filter warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning

# Minimum version
minversion = 6.0

# Test timeout (seconds)
timeout = 300 